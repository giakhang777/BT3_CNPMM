<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Create User</title>
  <style>
    body { font-family: sans-serif; margin: 24px; }
    nav a { margin-right: 12px; text-decoration: none; color: #06c; }
    h1 { margin: 12px 0 16px; }
    form { max-width: 640px; display: grid; grid-template-columns: 1fr 1fr; gap: 10px; }
    form input, form select { padding: 8px; }
    form .full { grid-column: 1 / -1; }
    button { padding: 10px 14px; }
    .container { max-width: 980px; margin: 0 auto; }
  </style>
</head>
<body>
  <div class="container">
    <nav>
      <a href="/home">Home</a>
      <a href="/crud"><b>Create</b></a>
      <a href="/get-crud">List Users</a>
    </nav>

    <h1>Create User</h1>
    <form action="/post-crud" method="post">
      <input name="email" type="email" placeholder="Email" required class="full" />
      <input name="password" type="password" placeholder="Password" required class="full" />

      <input name="firstName" placeholder="First Name" />
      <input name="lastName"  placeholder="Last Name" />
      <input name="phoneNumber" placeholder="Phone Number" />
      <input name="address"     placeholder="Address" class="full" />

      <label>
        Gender
        <select name="gender">
          <option value="false">Female</option>
          <option value="true">Male</option>
        </select>
      </label>

      <input name="roleId"     placeholder="Role ID" />
      <input name="positionId" placeholder="Position ID" />
      <input name="image"      placeholder="Image URL" class="full" />

      <div class="full">
        <button type="submit">Create</button>
        <a href="/get-crud" style="margin-left:8px;">Cancel</a>
      </div>
    </form>
  </div>
</body>
</html>
